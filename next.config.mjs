/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["picsum.photos", "images.unsplash.com", "assets.aceternity.com","ap-south-1.graphassets.com"], // Add any other domains you plan to use here
  },
};

export default nextConfig;
